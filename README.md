# Example API server using Express & TypeScript

To build & run:

    npm install
    build
    npm start

Look at src/index.ts to see how to start an express server, along with a few
example API endpoints.
